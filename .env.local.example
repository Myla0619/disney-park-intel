# ─── AI 助手凭证（三选一，否则 Agent 不可用、评分降级为本地规则）──────────
#
# 方式一（本机开发推荐，不产生长期密钥）：
#   brew install anthropics/tap/ant
#   ant auth login
#   凭证存到 ~/.config/anthropic/，SDK 自动读取，本文件里什么都不用填。
#
# 方式二（CI / 服务器 / 容器）：Workload Identity Federation，同样无长期密钥。
#   需同时设置 ANTHROPIC_FEDERATION_RULE_ID / ANTHROPIC_ORGANIZATION_ID /
#   ANTHROPIC_SERVICE_ACCOUNT_ID / ANTHROPIC_IDENTITY_TOKEN_FILE，缺一不激活。
#
# 方式三：静态 API key，https://console.anthropic.com 获取。
#
# ⚠️ 陷阱：ANTHROPIC_API_KEY 优先级最高，且**留空也算数**——空值会以空密钥发起
# 认证并屏蔽掉配置档与联合身份凭证。不用它就把整行删掉，不要留成空值。
ANTHROPIC_API_KEY=

# 模型可覆盖，默认 claude-opus-5
# CLAUDE_SCORING_MODEL=
# CLAUDE_ITINERARY_MODEL=
# CLAUDE_AGENT_MODEL=

# ─── 小红书评论抓取（仅离线采集脚本需要，线上不需要）──────────────────────
# 语料由 `node scripts/collect_reviews.mjs` 抓取后提交进 data/reviews/，
# 运行时直接读仓库里的语料，因此部署环境不必配置此项。
# 注册 https://apify.com（有免费额度），Settings → API & Integrations 取 token
APIFY_TOKEN=
# 可替换为其他 actor（默认见 src/lib/sources/xiaohongshu.ts）
# APIFY_XHS_SEARCH_ACTOR=
# APIFY_XHS_DETAIL_ACTOR=

# ─── TripAdvisor 评论（选填，作为小红书之外的补充来源）────────────────────
# https://rapidapi.com 订阅 TripAdvisor API（有免费额度）
RAPIDAPI_KEY=

# ─── 会话持久化（选填但生产强烈建议）──────────────────────────────────────
# 不配置时会话存在进程内存：Serverless 每个实例各存一份、冷启动即丢失，
# 多轮对话的偏好记忆在生产环境实际不生效。
# https://upstash.com 创建 Redis 后取 REST URL 与 Token
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# ─── 限流阈值（选填）──────────────────────────────────────────────────────
# RATE_LIMIT_AGENT=20
# RATE_LIMIT_LLM=10
# RATE_LIMIT_DATA=120
